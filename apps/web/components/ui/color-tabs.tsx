'use client'

/**
 * ColorTabs — RE-EXPORT SHIM.
 *
 * 2026-06-10: a tényleges komponens átkerült a `@kartoteka/ui-app`-ba
 * (`packages/ui-app/src/layout/ColorTabs.tsx`), hogy a desktop is PONTOSAN
 * ugyanazt használja. Minden meglévő
 * `import { ColorTabs } from '@/components/ui/color-tabs'` változatlanul működik.
 */

export { ColorTabs } from '@kartoteka/ui-app'
